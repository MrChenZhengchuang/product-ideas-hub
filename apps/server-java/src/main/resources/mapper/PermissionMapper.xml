<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.productideas.mapper.PermissionMapper">

  <select id="listByRoleIds" resultType="com.productideas.domain.PermissionItem">
    SELECT DISTINCT p.id, p.permission_code AS permissionCode
    FROM role_permissions rp
    INNER JOIN permissions p ON p.id = rp.permission_id
    WHERE rp.role_id IN
    <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
      #{roleId}
    </foreach>
    ORDER BY p.id ASC
  </select>

</mapper>
