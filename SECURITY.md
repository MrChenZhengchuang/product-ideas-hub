# Security Policy

## 报告安全问题

请不要在公开 Issue 中提交可利用的漏洞、密钥或用户数据。请通过仓库所有者在 GitHub 个人资料中公开的联系方式私下报告，并包含：

- 受影响的模块和版本
- 复现步骤
- 潜在影响
- 建议修复方式（如有）

维护者确认问题后会尽快给出处理进度。

## 部署提醒

- 必须替换默认 `JWT_SECRET` 和演示账号密码。
- 生产环境使用独立数据库账号，避免使用 `root`。
- 将 `CORS_ALLOWED_ORIGIN_PATTERN` 限制为真实前端域名。
- 上传目录应使用持久化存储，并在网关层限制文件类型和大小。
